<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PokeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ColorRed</item>
        <item name="colorPrimaryVariant">@color/ColorRedBrown</item>
        <item name="colorOnPrimary">@color/ColorWhite</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/ColorBlue</item>
        <item name="colorOnSecondary">@color/ColorYellow</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.PokeApp" parent="Base.Theme.PokeApp" />
</resources>